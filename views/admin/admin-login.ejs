<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skinova Login</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
        }

        body {
            min-height: 100vh;
            background-color: #1a1d21;
        }

        .container {
            min-height: 100vh;
            display: flex;
            flex-direction: row;
        }

        .left-section {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 2rem;
        }

        .logo {
            width: 350px;
        }

        .right-section {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 2rem;
        }

        .login-container {
            width: 100%;
            max-width: 400px;
        }

        .login-title {
            color: white;
            font-size: 1.75rem;
            margin-bottom: 2rem;
            font-weight: 500;
        }

        .form-group {
            margin-bottom: 1rem;
            text-align: left;
        }

        .form-label {
            display: block;
            color: #9ca3af;
            margin-bottom: 0.5rem;
            font-size: 0.875rem;
        }

        .form-input {
            width: 100%;
            padding: 0.75rem;
            border: 1px solid #374151;
            border-radius: 0.375rem;
            background-color: #1f2937;
            color: white;
            font-size: 0.875rem;
        }

        .form-input:focus {
            outline: none;
            border-color: #6366f1;
        }

        .login-button {
            width: 100%;
            padding: 0.75rem;
            margin-top: 1.5rem;
            border: none;
            border-radius: 0.375rem;
            background-color: #7c3aed;
            color: white;
            font-size: 0.875rem;
            cursor: pointer;
            transition: background-color 0.2s;
        }

        .login-button:hover {
            background-color: #6d28d9;
        }

        .error-message {
            color: #ef4444;
            margin-bottom: 1rem;
            font-size: 0.875rem;
        }

        @media (max-width: 768px) {
            .container {
                flex-direction: column;
            }

            .left-section {
                padding: 2rem;
            }

            .right-section {
                padding: 0 2rem 2rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <section class="left-section">
            <img src="/img/skinova-admin-logo.png" alt="Skinova Logo" class="logo">
        </section>
        
        <section class="right-section">
            <div class="login-container">
                <h1 class="login-title">Login</h1>

        

                <!-- Login Form -->
                <form action="/admin/login" method="post">
                    <div class="form-group">
                        <label for="email" class="form-label">Email</label>
                        <input 
                            type="email" 
                            id="email" 
                            name="email" 
                            class="form-input" 
                            required
                        >
                    </div>
                    <div class="form-group">
                        <label for="password" class="form-label">Password</label>
                        <input 
                            type="password" 
                            id="password" 
                            name="password" 
                            class="form-input" 
                            required
                        >
                    </div>

                    <button type="submit" class="login-button">Log in</button>
                </form>
            </div>
        </section>
    </div>
</body>
</html>